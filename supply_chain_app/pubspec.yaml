name: supply_chain_app
description: "A new Flutter project."
publish_to: 'none'  # Prevent accidental publishing

version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.4.0
  provider: ^6.0.0
  firebase_core: ^2.0.0
  cloud_firestore: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/image/wakdukaan_logo.png
    - assets/image/Airpods.png
    - assets/image/charger.png
    - assets/image/headsets.png
    - assets/image/ladies watch.png
    - assets/image/men watch.png

